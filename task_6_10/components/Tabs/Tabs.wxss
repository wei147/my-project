.tabs_titles{
  display: flex;
  border-bottom: 1rpx solid #ccc;
}

.title_item{
  flex: 1;
  padding: 15rpx 0;
  display: flex;
  justify-content: center;
  align-items: center;
}

.active{
  border-bottom: 4rpx solid var(--themColor);
}