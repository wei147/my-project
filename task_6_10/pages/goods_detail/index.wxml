<view class="detail_swiper">
  <swiper>
    <swiper-item wx:for="{{goodsObj.pics}}" wx:key="pics_id" bindtap="handlePreImg" data-url="{{item.pics.mid}}">
      <image mode="widthFix" src="{{item.pics.mid}}">
      </image>
    </swiper-item>
  </swiper>
</view>